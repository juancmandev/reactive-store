import { Typography, AppBar, Toolbar } from '@mui/material';

const Header = () => {
  return (
    <AppBar>
      <Toolbar>
        <Typography>Header</Typography>
      </Toolbar>
    </AppBar>
  );
};

export default Header;
